# Essential packages first
setuptools>=65.0.0
wheel>=0.38.0
pip>=22.0.0

# Web Crawling and HTTP Requests (using more stable versions)
requests>=2.28.0,<3.0.0
aiohttp==3.9.1
httpx>=0.24.0,<1.0.0

# HTML Parsing and Web Scraping
beautifulsoup4==4.12.2
lxml==4.9.3
html5lib>=1.1,<2.0.0

# Image Processing and Download
Pillow>=9.5.0,<11.0.0

# Data Processing and Storage
pandas>=1.5.0,<3.0.0
numpy>=1.24.0,<2.0.0

# Async and File Operations
aiofiles>=22.1.0,<24.0.0

# Browser Automation (optional)
playwright>=1.30.0,<2.0.0

# JSON and Configuration
python-dotenv>=0.19.0,<2.0.0

# Logging and Utilities
loguru>=0.6.0,<1.0.0
tqdm>=4.64.0,<5.0.0

# Rate Limiting and Delays
tenacity>=8.0.0,<9.0.0

# Alternative to crawl4ai for now (we'll implement custom crawler)
# crawl4ai causes dependency conflicts, so we'll build our own solution